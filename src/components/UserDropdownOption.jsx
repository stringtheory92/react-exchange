import React from "react";

function UserDropdownOption({
  user,
  btcPrice,
  updateBtcPrice,
  ethPrice,
  updateEthPrice,
}) {
  return (
    <div>
      {/* <option value={Object.keys(token)}>{`${Object.keys(
        token
      )[0].toUpperCase()}: ${Object.values(token)[0]}`}</option> */}
    </div>
  );
}

export default UserDropdownOption;
